import BackgroundResults from "./BackgroundResults";

interface BackgroundClass {
    count: number;
    next: null;
    previous: null;
    results: BackgroundResults[]
}

export default BackgroundClass